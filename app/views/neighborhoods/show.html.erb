<div class="col-md-6 col-md-offset-3">
	<h2>You have <%= @hood.rides.count %> rides</h2>

  <% @hood.rides.each do |ride| %>
    <li><%= link_to ride.title, [ride.hood, ride] %></li>
  <% end %>
</div>