<div class="col-md-6 col-md-offset-3">

	<div class="alert alert-info">
		You have <%= @rides.count %> rides
	</div>

  <% @rides.each do |ride| %>
    <li><%= link_to ride.title, [ride.hood, ride] %></li>
  <% end %>
  Test
</div>